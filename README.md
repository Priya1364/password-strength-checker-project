# password-strength-checker-project
A simple Python program that checks the strength of a user-entered password based on multiple conditions like length, digits, uppercase, lowercase, and special characters. Built using basic Python concepts such as loops, conditionals, and functions.
